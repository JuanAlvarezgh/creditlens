name: creditlens
version: '1.0.0'
config-version: 2

profile: creditlens

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  creditlens:
    raw:
      +materialized: view
    staging:
      +materialized: view
    mart:
      +materialized: table
